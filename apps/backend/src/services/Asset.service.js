/**
 * Get all accounts.
 *
 * @returns
 */
function get() {
  return true;
}

/**
 * Add acc balance.
 *
 * @param address
 * @returns
 */
async function create() {
  return true;
}

/**
 * Add acc balance.
 *
 * @param address
 * @returns
 */
async function update() {
  return true;
}

/**
 * Add acc balance.
 *
 * @param address
 * @returns
 */
async function transfer() {
  return true;
}

export default {
  get,
  create,
  update,
  transfer,
};
